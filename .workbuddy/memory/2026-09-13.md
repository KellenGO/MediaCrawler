# 2026-09-13

## site/ 落地页优化（三批全部完成）
- 硬伤：`section[id]{scroll-margin-top:84px}` 修锚点被 sticky 顶栏遮挡；`.reveal` 显现改为 `html.js` 前缀（首帧脚本 `classList.add('js')`），JS 失效时页面不再空白；下载 CTA 改稳定直链 `releases/latest/download/MediaCrawler-Windows-x64.zip`（与 release-package.yml 产物名一致，共 4 处：JSON-LD downloadUrl、顶栏、hero、移动端底条），面板"前往 Releases"保留 releases 页（拿 sha256）。
- 转化：删除 hero 冗余 `.home-note`；12 张功能卡改为 9 张保留 + 3 张（摘要补全与缓存/搜索统计与限流/账号诊断）收进 `<details class="more">` 折叠区（复用 FAQ chevron 样式）；"三种排序"卡文案缩短。
- 小修：导航加"许可"；平台表知乎说明改为"搜索结果不含收藏数；收藏夹名称会保留"；#features、#faq 加 `tint`（--soft 底色）交替；JSON-LD 补 softwareVersion 0.1.0 / releaseNotes / screenshot；删除 .home-note 死 CSS。
- og.png 已用 `scripts/build_landing_og.py`（venv python，msedge channel）重新生成。
- 验证：html.parser 标签平衡 OK；Playwright 冒烟无 JS 错误、折叠区 3 卡、锚点 top=84.5px。
- 教训：同一文件多个 Edit 绝不能并行调用，会互相覆盖（本次 6 个并行 Edit 只保留 1 个，靠复验脚本抓回）。
- 遗留：仓库尚无 GitHub Release，所有下载按钮在 Release 发布前是 404（README 已注明）；首屏 CSS mock 建议后续换真实截图并重跑 og 脚本。

## 首屏换真实截图（同日下午）
- 用户提供 4 张 App 截图（D:\图片\Snipaste_2026-09-13_*.png，2550×1233 HiDPI）。选用结果页截图（关键词"拍摄技巧"），其余三张（空首页/最近搜索/收藏页）未用。
- 用 Pillow 处理：按像素扫描（灰度阈值 130/200 逐行统计暗像素）精确定位两条结果的 metadata 行 y 坐标（867-882、1058-1072），白色矩形抹除作者名与互动数据；裁掉底部截断的第三条结果（顺带去掉带人脸缩略图）→ 输出 site/shot-search.png（2550×1116）。
- index.html：.mock CSS 复刻块整段删除（约 65 行），换成 <img class="shot" src="shot-search.png">（新增 .shot 样式），图注改为"真实界面…（截图已隐去作者与互动数据）"，文件 1290→1157 行。顺手清理 prefers-reduced-motion 里残留的 .mock-search 规则，并把该块 .reveal 改为 html.js .reveal 保证特异性。
- og.png 重跑脚本，分享卡图现在含真实界面。README"换成真实截图"一节改写为已完成 + 截图处理步骤（供下次更新照做），自检清单勾掉第 4 项。
- 经验：抹图文字别靠目测坐标，用 numpy 逐行扫描暗像素带定位文字行最可靠；目测换算 HiDPI 缩放误差很大。
