# 决策：手动取消收藏同步

- 日期：2026-09-14
- 状态：**已实施**

## 背景

同步目标增加到每平台 100 条，用户需要中途停止。

## 选项

只停止前端等待，或者停止后端任务并保存已经收到的内容。

## 决定

取消会停止当前任务尚未完成的所有平台，等工作进程退出、部分结果保存完成后再确认。重复取消同一任务不会重复打断保存。按钮等待期间显示「正在取消」。

## 后果

取消不撤回已经发出的平台请求，不删除历史收藏；磁盘保存可能使确认略有延迟。请求失败时可以重试，主动取消不被提示为账号故障。
