# 合并后测试版交付

状态：本机测试候选，等待用户实测；未发布 Release、未推送标签。

## 构建与入口

- 程序源码：master `5b5929b81b6420094e75e2525dfd6e46b815a2d2`，已包含结构优化与账号、收藏行为修复。
- 本次候选版本：`0.2.0`。此版本号来自项目配置，正式版本仍以 git tag 为准。
- 唯一日常测试入口：`dist/MediaCrawler/四野.exe`，根目录 `MediaCrawler.bat` 优先打开它。
- 可分发候选：`dist/MediaCrawler-Windows-x64.zip`，174,804,534 字节。
- ZIP SHA256：`7a0d50281a2974f966e39c3a5d1e78f19069e529e9eb14126956fecd17e2eaa3`。
- 包内 `TEST_BUILD.json` 记录源码提交与构建时间；本次后续提交仅更新验证脚本和文档，未修改产品代码。
- 验证期间另有提交 `f19baa9` 更新介绍页和 README 的 Android 入口。它未改桌面程序；此候选包的 README 仍来自构建时的 `5b5929b`，若决定公开发布，应从确认后的最终提交重新构建。

## 验证证据

- 收尾前／收尾后的后端全量测试均为：971 passed、5 skipped、0 failed，无新增失败。
- 收尾前／收尾后的前端均为：331 passed、0 failed；应用与测试 TypeScript 编译、Vite 构建通过。
- `favorites_ui_smoke.py`：对源码构建及最终 dist 内的前端分别通过；覆盖收藏迁移、收藏夹、备注写入失败及重试、刷新持久化、同步取消与重试、窄屏布局。
- `result_library_smoke.py`：已适配当前 hash 页面与 SQLite 收藏 API；通过关键词筛选、高亮、来源选择、详情展开、收藏与备注刷新、取消编辑、CSV／Markdown 导出、复制链接、写入失败反馈；390／1024／1440 宽度无横向溢出。
- 浏览器检查使用临时数据库、模拟账号和平台响应，阻止真实平台请求。实际查看了收藏桌面截图、备注阅读截图及 390px 深色收藏页截图；自动布局断言不代表所有页面均已人工视觉验收。
- 干净环境 EXE 检查通过：不依赖系统 Python／Node、worker 协议、页面及静态资源、安全路径拒绝、后台正常退出。
- ZIP 完整性与校验和通过，排除了用户收藏、登录资料和缓存。OpenCV 自带的 `_internal/cv2/data` 是依赖资源，不是用户数据。
- 替换前后 2,733 个用户数据文件 SHA256 全部一致；最终入口的 682 个程序文件与已验证构建逐一一致。
- 本机日志保存在忽略提交的 `build/delivery-*.log`，截图同在 `build/`。

## 已知边界与用户实测

1. 真实平台的登录确认、搜索及收藏同步仍需使用者实测。模拟响应不能证明小红书等平台此时一定允许操作。
2. 请检查账号页首次打开、重新验证和搜索前后的状态是否符合实际；收藏同步时应同时显示加载与取消操作，取消后已有内容保留，重启仍能看到本地内容。
3. 检查超长收藏夹名称、重命名及删除入口；展开长结果应增加整条结果高度，收起后恢复整齐列表。
4. 托盘菜单真实点击、无控制台窗口及退出体验仍需用户实机确认。
5. 保留现有 FastAPI 生命周期弃用、缓存协程警告，以及 Vite 的浏览器兼容数据陈旧和大分块提示；本次未扩展范围修改这些问题。
6. 本次只完成本地交付，不将测试通过等同于公开发布许可。
