# <功能名>

> 用法：复制本文件为 `docs/features/<功能>.md`，填完后删掉这一段。
> **一个功能一个文件**——这样多个分支/agent 并行时不会互相冲突。

## 一句话

（这个功能做什么、给谁用。一两句，不要贴代码。）

## 代码入口

| 职责 | 位置 |
|---|---|
|  | `path/to/file.py` |

## 关键决定

- （为什么这么做。有取舍就写进 `docs/decisions/` 并在这里链接。）

## 已知坑 / 边界

- （还没做的、有风险的、刻意不做的。这里最有价值——写清楚下次不用重新踩。）

## 测试怎么跑

（测试文件路径 + 跑法。通用跑法见根目录 `AGENTS.md`。）
