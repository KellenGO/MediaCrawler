优化与精简（P0 全部 + P1 一部分）：删掉 fork 遗留死代码、收敛重复实现

按 docs/plans/2026-09-14-优化与精简方案.md 执行，净减 1484 行（92 files, +214/-1698）。
全程每阶段跑全量测试：后端 958→953 passed / 0 failed（少的 6 passed + 3 skipped
是被删的两个 proxy 测试文件），前端 330 passed / 0 failed 未变。

P0 零风险清理（全部完成）
- 删除 IP 代理池整包 proxy/（含 proxy_mixin.py，比原计划更彻底）：开关 ENABLE_IP_PROXY
  恒为 False 且 worker 四条主路径各自再强制赋 False，所有调用点都不可达。
  连带清理 4 个 client 的 mixin 基类/参数/刷新调用、4 个 core 的初始化块与传参、
  tools/crawler_util.format_proxy_info、result_hydration 的 3 处显式 None、
  spec 的 collect_submodules("proxy")、base_config 三项池配置、2 个测试文件。
- 删除两个从未被 import 的死依赖 cryptography / websockets（requirements / pyproject /
  MediaCrawler.spec 三处同改，否则 test_repo_hygiene 的奇偶校验立刻失败）。
  websockets 一直写在 spec 的 hiddenimports 里，被打进发行包。
- 删除 启动.bat（与 MediaCrawler.bat 逐字节相同的重复入口）、
  死组件 webui/src/components/search/SearchStatistics.tsx、
  从未被执行的 mypy.ini、3 份从未被抛出的僵尸 IPBlockError。
- 删除已验证的死代码：accounts._PROJECT_ROOT（连带未用导入）、未使用的 _state_lock、
  result_hydration 的 self._locks、routers/search 未使用的 background_tasks 参数。
- .gitignore 补 .tmp-pytest/（连字符版），scripts/package_windows.py 打包清单去掉 proxy。

P1 去重与抽公共层（部分完成）
- 新增 api/services/sqlite_base.py：RESULT_FIELDS / default_db_path / utc_now /
  SqliteStoreBase（连接、线程内复用、BEGIN IMMEDIATE、WAL 只在建库时开一次）。
  library_store 与 remote_favorites_store 改为继承，消除两份逐字重复的 _now/_conn/单例样板。
- 四个平台 pong 签名统一为 (*, raise_on_error=False, browser_context=None)，
  删掉 aggregate_search/worker.py 里用 inspect.signature 反射猜签名的 6 行。
- 新增 webui/src/lib/platformMeta.ts 作为平台列表与 slug 守卫的唯一来源：
  该数组原先在 8 个文件里各写一份（含 searchExperience 自己的 isPlatformSlug），
  现在以 re-export/别名保留旧名字，调用点无需改动。
- pytest 配置加 pythonpath=["."] 与 testpaths，删掉 30 个测试文件里的 sys.path.insert 样板。
- 删 tests/conftest.py 的 3 个孤儿 fixture 与引用了已删变量的 project_root_path。
- 5 个 smoke/probe 脚本入口统一成 raise SystemExit(main())。

wiki 与制度
- 新增 3 份功能文档：search-experience.md、environment-health.md、license-disclaimer.md
  （覆盖此前 16 个无文档功能里最主要的三块，含此前风险最高的许可/合规面）。
- 补上缺失的 decisions/2026-09-14-后端端口可配置.md。
- tests/test_docs_wiki.py 新增断言「feature 文档代码入口里写的文件必须存在」——
  它当场抓出 4 处已漂移的入口与 3 处写法不规范；AGENTS.md 同步要求「代码入口不要写行号」。
- 修正 6 处用户可见的错误说明（README、docs/使用说明.md、site/guide.html ×2、
  site/index.html ×2）：收藏早已迁到本机 SQLite，不再"清缓存就丢"。
- 修正 4 处代码入口漂移（漏掉的 cancel 路由、写死的路由数量、漂移的行号）。
- AGENTS.md 点明 .workbuddy/ 被 gitignore，流水账那条 DoD 只在本机有效。
- 根目录 交接-feat-scan-login.md 归档为 docs/history/2026-09-14-扫码登录交接.md。

未做（记录在方案 §0.0）：P1 剩余的 worker_process 抽取、409 守卫、错误协议统一、
两个平台基类、前端抽件与 i18n 接线；P2 四个 God module 拆分；§9 十项基础建设。
